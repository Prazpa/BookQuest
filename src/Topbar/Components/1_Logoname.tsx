const Logoname = () => {
  const reponsive = ``
  const customtext = `text-teal-600 text-2xl font-bold font-['overpass']`
  return (
    <div className={`${customtext} ${reponsive}`}>
      <a href="http://localhost:5173/">BookQuest</a>
    </div>
  );
};

export default Logoname;
